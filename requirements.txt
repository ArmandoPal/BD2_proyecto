fastapi
uvicorn[standard]
pydantic
python-multipart
pandas          # solo para generate_dataset.py / benchmarks, no para el motor
matplotlib      # gráficas de benchmarks
numpy           # utilidades numéricas en benchmarks
pytest          # tests
httpx           # TestClient de FastAPI en los tests de la API
